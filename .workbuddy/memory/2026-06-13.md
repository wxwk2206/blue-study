# 2026-06-13 工作日志

## 笔记维护 — Java基础 文件夹添加本篇关联
- 为 39 个笔记文件末尾添加 Obsidian callout 格式的「本篇关联」
- v1：机械线性链（前一篇←当前→后一篇），处理 34 个，5 个跳过
- v2：用户要求按内容逻辑重做 → 通读全部 39 篇，按知识点真正关联性建立链接
- v3：修复 wikilink 路径反斜杠问题 — os.path.join 在 Windows 生成 `\`，Obsidian 只认 `/`，全部替换，39 个文件修复
  - 跨组关联示例：03.数组 → 02.集合ArrayList（定长vs变长）、3.JDBC → 4.MyBatis增删改查（#{}占位符→预编译）
  - 跨层关联示例：01.语法 → 01.字符串String（String是引用类型）、03.IOC与DI → 04.抽象类接口（接口是实现DI的关键）
  - 安全交叉：3.DQL → 3.JDBC（SQL注入→预编译防护）
