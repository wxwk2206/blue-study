# 2026-06-21 工作日志

## Quiet Outline 插件展开 Bug 排查与修复

**问题**：Quiet Outline 设置 `expand_level: 5`（默认展开到 h5），但部分文件打开时层级不展开。

**插件信息**：obsidian-quiet-outline v0.5.14，位于 `.obsidian/plugins/obsidian-quiet-outline/`

**排查过程**：
1. 读取 `data.json` 配置，发现关键三项：`expand_level:"5"`、`persist_md_states:true`、`auto_expand_ext:"disable"`
2. 发现 `markdown-states.json`（133KB）缓存了每个文件的 `expandedKeys` 数组，大部分文件是 `expandedKeys: []`（空数组=全折叠）
3. 扒 main.js 源码，定位核心判断逻辑：
   ```js
   n.settings.persist_md_states && r ? _(r) : h(m.value)
   // persist 开启 且 有存储记录 → 恢复存储状态；否则用 expand_level
   ```
4. 根因：存储逻辑兜底 `|| []`，而 JS 中空数组 `[]` 是 truthy，导致已打开过的文件永远走恢复分支，`expand_level` 被完全忽略

**修复**：将 `data.json` 中 `persist_md_states` 从 `true` 改为 `false`。代价是失去"记住每个文件展开状态"功能，但 `expand_level: 5` 可稳定生效。

**关键文件**：
- 配置：`.obsidian/plugins/obsidian-quiet-outline/data.json`
- 状态缓存：`.obsidian/plugins/obsidian-quiet-outline/markdown-states.json`
- 源码：`.obsidian/plugins/obsidian-quiet-outline/main.js`（压缩，618KB）

**教训**：上次凭直觉编了"时序竞争"理论，没看源码。这次真正读 main.js 才定位到 `[]` truthy 这个 JS 陷阱。排查插件 bug 必须读源码，别猜。

## Java 面向对象笔记 mermaid 转 SVG 图片

**问题**：`06.面向对象/` 下 3 个 md 文件共 7 个 mermaid 代码块在 Obsidian 里不显示（渲染失败）。

**做法**：手写 SVG 替换 mermaid 代码块，绕开 Obsidian mermaid 渲染问题。
- 新建目录：`06.面向对象/mermaid-img/`
- 生成 7 个 SVG 文件：01-对象创建内存过程、03-1-Person继承关系、03-2-多层继承链、03-3-成员变量就近原则、03-4-super调用时序图、03-5-多态类图、04-接口实现关系
- md 中用 `![描述](mermaid-img/xxx.svg)` 相对路径引用
- SVG 颜色按 read_me 的 9 色 ramp 体系（c-blue/c-purple/c-teal 等），浅色填充+深色描边+深色文字

**踩坑**：跨会话续接时，上一会话 Write 工具返回成功但文件未真正持久化（只剩 04 一个文件），md 引用变成死链。续做时必须 ls 验证产物真实存在，不能轻信工具返回的成功消息。

**待办**：02.static、final与枚举.md 无 mermaid，未处理。其它目录若也有 mermaid 渲染问题，可同法处理。

## C 语言 day8 结构体笔记整理

**任务**：用户在桌面放了 day8-1.c ~ day8-4.c + stu.txt 共 5 个文件，要求按 obsidian-format 技能整理为笔记，输出到 `代码学习/C语言/`。

**4 个文件的主题串联**：
- day8-1：结构体基础定义 + 结构体数组 + strcpy 赋值
- day8-2：typedef 起别名 + 4 字节内存对齐 + sizeof
- day8-3：`.` vs `->` 操作符 + CRUD 函数封装 + 地址传递
- day8-4：文件操作三步走 fopen/fwrite/fclose + 手写 myitoa/ftoa + fgets+strstr 查找 + `#if 0` 条件编译

**做法**：发现 `06.结构体.md` 已存在但只有一行字"不同数据类型的集合"，直接覆盖补全。按 obsidian-format 规范加 frontmatter（title/date/categories/tags）、Callout（abstract/tip/important/warning/bug/note）、表格对比、代码块带 ⭐ 关键注释、文末"本篇关联"链接到 [[03. 指针]] [[04. 内存管理]] [[05. 指针进阶]] [[02. 数组、字符串与函数基础]]。

**产物**：`D:\code\学习笔记\代码学习\C语言\06.结构体.md`（覆盖原空文件）

## C 语言 day8-4 文件操作独立笔记

**任务**：用户反馈 day8-4.c 是一个整体（文件 I/O 完整 demo），不能肢解到结构体笔记里。要求整体输出为独立笔记并附带解释。

**做法**：新建 `07.文件操作.md`（C 语言目录下序号 07）。整体代码原样放入代码块（保留 `#if 0` 屏蔽的 demo），分 6 个模块逐块解释：结构体定义 / myitoa / ftoa / record2file / search_stu / main / #if 0 demo。

**重点标注**：
- `myitoa` / `ftoa` 的 ASCII 转换公式（数字→字符 `n + 48`），并指出"只支持两位数"的局限
- `search_stu` 里 `while(cfp != (FILE*)EOF)` 的 bug：文件指针永远不会等于 EOF，应改用 `while(fgets(...) != NULL)`，并给出修正版
- 主流程"先 fclose 再 fopen"的必要性（位置指针在末尾），以及 `rewind` 替代方案
- fopen 模式速查表（r/w/a/r+/w+/a+）
- 改进点对照表（sprintf 替代手写转换、rewind 替代关文件重开等）

**产物**：`D:\code\学习笔记\代码学习\C语言\07.文件操作.md`（已废弃，见下方调整）

## 调整：day8-4 合并回 06.结构体.md

**用户反馈**：day8-4 不要单独成篇，整体合并到 06.结构体.md 里。

**做法**：
1. 把 06.结构体.md 原第四章（4.1-4.5 简化版）整体替换为新版"四、文件操作与数据持久化"：
   - 4.1 整体代码（day8-4.c 完整原样放入）
   - 4.2 逐模块解释（结构体定义/myitoa/ftoa/record2file/search_stu/main/#if 0 demo）
   - 4.3 生成的数据文件 stu.txt
   - 4.4 fopen 模式速查表
   - 4.5 文件操作三步走总结
   - 4.6 潜在改进点对照表
2. 重点标注 search_stu 的 `while(cfp != (FILE*)EOF)` bug（`[!danger]` callout），给出 `fgets(...) != NULL` 修正版
3. tags 增加 "数据持久化"、"ASCII转换"
4. 今日核心总结加第 5 条：循环条件 bug 教训
5. 删除独立的 07.文件操作.md

**产物**：`D:\code\学习笔记\代码学习\C语言\06.结构体.md`（含全部 day8 四节内容）
