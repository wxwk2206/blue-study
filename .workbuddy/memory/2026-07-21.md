# 2026-07-21

## Obsidian：让 PNG 文件夹不计入文件树总数
- 问题：vault 根目录 `PNG/` 有 841 张图，被文件树计数算进"笔记总数"。
- 方案：新增 CSS snippet 隐藏该文件夹（仅视觉，不影响引用）。
- 文件：`D:\code\学习笔记\.obsidian\snippets\hide-png-folder.css`
- 启用：`.obsidian\appearance.json` 加 `cssSnippets: ["hide-png-folder"]`
- 备注：Obsidian 原生无"排除文件夹计数"开关；替代方案为移出 vault 外部或重命名为 `.PNG`。

## 21:26 用户要求撤销
- 已删除 `.obsidian/app.css`（隐藏 PNG 的样式）。
- 之前 snippet 文件上一轮已删，appearance.json 无残留 cssSnippets 字段。
- 现状：vault 配置回到最初，PNG 文件夹正常显示。
- 教训：Obsidian 开着时手动改 appearance.json 的 cssSnippets 会被覆盖；若再用 CSS 方案应走界面「CSS 片段」开关或 app.css（但用户本次选择撤销）。
