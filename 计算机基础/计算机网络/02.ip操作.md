
```java
package com.qiming;

import java.net.InetAddress;

public class App {
    public static void main( String[] args ){
        //InetAddress是Java提供的操作IP地址的类
        //InetAddress类对象智能通过调用静态方法得到
        try {
            /*
            //得到封装有本机ip和主机名的InetAddress类对象
            InetAddress benjiAddress =  InetAddress.getLocalHost();
            //得到本机ip的String类型
            String  benjiip = benjiAddress.getHostAddress();
            System.out.println("本机ip的String类型=="+benjiip);
            //得到本机的主机名
            String  benjiming = benjiAddress.getHostName();
            System.out.println("本机的主机名=="+benjiming);
            */

            //得到封装有百度的ip地址对象
            InetAddress baiduAddress = InetAddress.getByName("www.baidu.com");
            //得到本机ip的String类型
            String  baiduip = baiduAddress.getHostAddress();
            System.out.println("百度ip==="+baiduip);
            String  baiduzhujiming = baiduAddress.getHostName();
            System.out.println("百度主机名==="+baiduzhujiming);
        }catch (Exception e){
            e.printStackTrace();
        }
    }
}
```
