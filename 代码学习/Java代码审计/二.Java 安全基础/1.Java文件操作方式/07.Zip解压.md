# 前言
```plain
做Java代码审计的读者在挖后台getshell漏洞时,可以注意一下解压缩的漏洞

例如:
在代码审计的时候,搜索ZipInputStream与ZipEntry这两个关键字
然后查看是否限制了路径,是否可以路径穿越,达到解压缩的时候释放一个木马到系统的根路径下
```

# ZipInputStream与ZipEntry组合
## 例子
```plain
第一步
制作一个ZIP,名字就叫test.zip
test.zip里面文件为1.txt
1.txt内容为123456
```

```java
// 第二步
// 添加一个解压的工具包
import java.io.BufferedOutputStream;
import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;
import java.util.zip.ZipEntry;
import java.util.zip.ZipInputStream;

/**
 * 本工具类解压标准的Zip包到指定目录中
 */
public class UnZipUtility {
    /**
     * 读/写数据时缓冲区大小
     */
    private static final int BUFFER_SIZE = 4096;

    /**
     * 解压有zipFilePath所指定的Zip文件到destDirectory所指定的目录（如果目标目录不存在将会重新创建）
     *
     * @param zipFilePath   压缩文件路径
     * @param UnzipFilePath 解压缩文件路径
     * @throws IOException
     */
    public void unzip(String zipFilePath, String UnzipFilePath) throws IOException {
        File destDir = new File(UnzipFilePath);
        if (!destDir.exists()) {
            destDir.mkdir();
        }

        ZipInputStream zipIn = new ZipInputStream(new FileInputStream(zipFilePath));

        ZipEntry entry = zipIn.getNextEntry();

        // 遍历Zip文件中的条目
        while (entry != null) {
            String filePath = UnzipFilePath + File.separator + entry.getName();
            if (!entry.isDirectory()) {
                // 如果条目是文件直接解压
                extractFile(zipIn, filePath);
            } else {
                // 如果条目是目录, 创建对应的目录
                File dir = new File(filePath);
                dir.mkdir();
            }
            zipIn.closeEntry();
            entry = zipIn.getNextEntry();
        }
        zipIn.close();
    }

    /**
     * 解压Zip包的条目 (文件条目)
     *
     * @param zipIn
     * @param filePath
     * @throws IOException
     */
    private void extractFile(ZipInputStream zipIn, String filePath) throws IOException {
        BufferedOutputStream bos = new BufferedOutputStream(new FileOutputStream(filePath));
        byte[] bytesIn = new byte[BUFFER_SIZE];
        int read = 0;
        while ((read = zipIn.read(bytesIn)) != -1) {
            bos.write(bytesIn, 0, read);
        }
        bos.close();
    }
}
```

```java
// 第三步
// 添加一个测试类
import java.io.IOException;

public class ZipTest {
    public static void main(String[] args) {
        try {
            new UnZipUtility().unzip(""C:\Users\22063\Desktop\test.zip.zip"", "./");
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/62702379/1770047937433-64a6068a-9166-49f8-9f93-eeca6d612704.png)<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/62702379/1770047946888-d3c68f05-9745-4447-92d4-5606a218866a.png)

